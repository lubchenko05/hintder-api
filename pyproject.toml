[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "dating-api"
version = "0.0.0"
description = "hintder backend — dating wingman API"
requires-python = ">=3.12"

[tool.black]
line-length = 100
target-version = ["py312"]
